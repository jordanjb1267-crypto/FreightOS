# FreightOS agent registry — operational copy.
#
# GENERATED by scripts/generate-agent-registry.mjs from
# docs/production-handoff/v1.2/config/agents/registry.yaml.
# Do not hand-edit. Declared as an authorised override in handoff-provenance.json.
#
# Why this file differs from the handoff (ADR-0015, ADR-0018):
#   - Every handoff entry carried 4 keys; agent-manifest.schema.json requires 12. All 32 entries
#     now validate against that schema.
#   - `max_autonomy` -> `maximum_autonomy`; `prohibited` -> `prohibited_actions`.
#   - `planes` -> `legal_authority_class` + `operating_contexts`.
#   - Agents the handoff declared on both the carrier and brokerage planes keep only the carrier
#     manifest. Brokerage is LEGAL_AND_MARKET_GATED and disabled in Horizon 1; its variant is
#     authored separately when the brokerage legal gate is signed.
#
# READ THIS BEFORE TRUSTING maximum_autonomy:
#   `maximum_autonomy` is a DECLARED ceiling, not a grant. The effective ceiling is
#   min(declared, module ceiling, horizon ceiling) — see effectiveMaximumAutonomy() in
#   packages/config and ADR-0018. In Horizon 1 every carrier agent resolves to A3 or lower and
#   every deferred-module agent resolves to A0, whatever this file says.
#
#   `allowed_tools` is empty for every agent. Empty means NO tool may be called. The tool
#   registry and per-tool schemas are Phase 2 deliverables.
#
#   `evaluation_suite` names the file each agent's evaluations will occupy. Those files do not
#   exist yet; they are Phase 2 deliverables.
#
#   `owner` is the repository owner pending per-agent accountable owners, assigned in Phase 2.

version: 1.2.0
generated_from: docs/production-handoff/v1.2/config/agents/registry.yaml
schema: schemas/agent-manifest.schema.json

agents:
  - id: chief-dispatch-orchestrator
    version: '0.1.0'
    purpose: "Coordinates carrier dispatch workflows across capacity, planning, and execution agents."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - contract.amend
      - money.move
      - traffic.allocate_cross_carrier
    evaluation_suite: evals/chief-dispatch-orchestrator.eval.yaml
  - id: capacity-agent
    version: '0.1.0'
    purpose: "Maintains the carrier view of available drivers, powered units, and equipment."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - assignment.commit
    evaluation_suite: evals/capacity-agent.eval.yaml
  - id: load-discovery-agent
    version: '0.1.0'
    purpose: "Ingests and normalises load opportunities from approved broker and board sources."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - load.accept
    evaluation_suite: evals/load-discovery-agent.eval.yaml
  - id: profitability-agent
    version: '0.1.0'
    purpose: "Applies deterministic cost profiles to score load profitability. Never calculates money itself."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A1
    allowed_tools: []
    prohibited_actions:
      - load.accept
      - rate.change
    evaluation_suite: evals/profitability-agent.eval.yaml
  - id: planning-agent
    version: '0.1.0'
    purpose: "Builds multi-load plans over available capacity and time windows."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - assignment.commit
    evaluation_suite: evals/planning-agent.eval.yaml
  - id: negotiation-agent
    version: '0.1.0'
    purpose: "Drafts rate negotiation and counteroffer language for human approval."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - contract.amend
      - policy.change
    evaluation_suite: evals/negotiation-agent.eval.yaml
  - id: feasibility-agent
    version: '0.1.0'
    purpose: "Checks hours, equipment, appointment, and facility feasibility for a candidate load."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A1
    allowed_tools: []
    prohibited_actions:
      - eligibility.override
    evaluation_suite: evals/feasibility-agent.eval.yaml
  - id: dispatch-agent
    version: '0.1.0'
    purpose: "Prepares driver dispatch instructions and assignment recommendations."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - traffic.allocate_cross_carrier
    evaluation_suite: evals/dispatch-agent.eval.yaml
  - id: tracking-agent
    version: '0.1.0'
    purpose: "Correlates tracking signals into shipment milestones and ETA observations."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - milestone.fabricate
    evaluation_suite: evals/tracking-agent.eval.yaml
  - id: exception-agent
    version: '0.1.0'
    purpose: "Detects and classifies shipment exceptions and prepares resolution options."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - claim.settle
      - risk_hold.release
    evaluation_suite: evals/exception-agent.eval.yaml
  - id: documentation-agent
    version: '0.1.0'
    purpose: "Extracts and validates shipment documents against expected structure."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - document.forge
    evaluation_suite: evals/documentation-agent.eval.yaml
  - id: settlement-agent
    version: '0.1.0'
    purpose: "Prepares invoices and post-load profit reconciliation for human review."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A3
    allowed_tools: []
    prohibited_actions:
      - bank.change
      - money.move
    evaluation_suite: evals/settlement-agent.eval.yaml
  - id: risk-agent
    version: '0.1.0'
    purpose: "Evaluates counterparty, cargo, and credit risk signals and raises holds."
    owner: rig-freightos-owner
    module: carrier_copilot
    legal_authority_class: carrier_agent
    operating_contexts:
      - carrier
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - risk_hold.release
    evaluation_suite: evals/risk-agent.eval.yaml
  - id: facility-operations-orchestrator
    version: '0.1.0'
    purpose: "Coordinates facility appointment, gate, dock, and receiving workflows."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - physical_motion.command
      - safety_interlock.override
    evaluation_suite: evals/facility-operations-orchestrator.eval.yaml
  - id: order-readiness-agent
    version: '0.1.0'
    purpose: "Tracks cargo readiness state ahead of a scheduled pickup appointment."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
      - shipper_owned
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - inventory.release_override
    evaluation_suite: evals/order-readiness-agent.eval.yaml
  - id: appointment-agent
    version: '0.1.0'
    purpose: "Proposes and confirms pickup and delivery appointments against facility capacity."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
      - shipper_owned
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - physical_motion.command
    evaluation_suite: evals/appointment-agent.eval.yaml
  - id: facility-capacity-agent
    version: '0.1.0'
    purpose: "Forecasts facility dock and labour capacity across appointment windows."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - capacity.fabricate
    evaluation_suite: evals/facility-capacity-agent.eval.yaml
  - id: gate-agent
    version: '0.1.0'
    purpose: "Manages gate credentials, arrival check-in, and departure check-out records."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - credential.security_override
      - physical_motion.command
    evaluation_suite: evals/gate-agent.eval.yaml
  - id: yard-orchestration-agent
    version: '0.1.0'
    purpose: "Recommends yard and staging assignments for vehicle visits."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A3
    allowed_tools: []
    prohibited_actions:
      - robot.control
      - yard_tractor.control
    evaluation_suite: evals/yard-orchestration-agent.eval.yaml
  - id: dock-agent
    version: '0.1.0'
    purpose: "Recommends dock door assignment and tracks loading and unloading progress."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A3
    allowed_tools: []
    prohibited_actions:
      - dock_restraint.control
      - door.control
      - plc.write
    evaluation_suite: evals/dock-agent.eval.yaml
  - id: load-verification-agent
    version: '0.1.0'
    purpose: "Verifies loaded cargo against consignment records and seal evidence."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - custody.fabricate
      - inspection.override
    evaluation_suite: evals/load-verification-agent.eval.yaml
  - id: receiving-agent
    version: '0.1.0'
    purpose: "Prepares goods receipt and delivery discrepancy records from receiving evidence."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
      - shipper_owned
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - inventory.acceptance.fabricate
    evaluation_suite: evals/receiving-agent.eval.yaml
  - id: facility-exception-agent
    version: '0.1.0'
    purpose: "Detects and classifies facility service exceptions and detention risk."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - claim.settle
      - safety_hold.release
    evaluation_suite: evals/facility-exception-agent.eval.yaml
  - id: labor-resource-planning-agent
    version: '0.1.0'
    purpose: "Forecasts facility labour and resource demand across shifts."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - employment.commit
    evaluation_suite: evals/labor-resource-planning-agent.eval.yaml
  - id: facility-customer-communication-agent
    version: '0.1.0'
    purpose: "Drafts facility status and exception communication for human approval."
    owner: rig-freightos-owner
    module: facilityos_lite
    legal_authority_class: software_only
    operating_contexts:
      - facility_operator
      - shipper_owned
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - contract.amend
    evaluation_suite: evals/facility-customer-communication-agent.eval.yaml
  - id: autonomous-mission-orchestrator
    version: '0.1.0'
    purpose: "Coordinates commercial autonomous mission requests. Never controls the dynamic driving task."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
    tenant_scope: isolated
    maximum_autonomy: A4
    allowed_tools: []
    prohibited_actions:
      - vehicle.accelerate
      - vehicle.brake
      - vehicle.change_lane
      - vehicle.reverse
      - vehicle.steer
    evaluation_suite: evals/autonomous-mission-orchestrator.eval.yaml
  - id: odd-eligibility-agent
    version: '0.1.0'
    purpose: "Consumes provider-authoritative ODD decisions. It cannot grant eligibility itself."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
    tenant_scope: isolated
    maximum_autonomy: A1
    allowed_tools: []
    prohibited_actions:
      - mission.authorize_without_provider
      - odd.override
    evaluation_suite: evals/odd-eligibility-agent.eval.yaml
  - id: vehicle-facility-compatibility-agent
    version: '0.1.0'
    purpose: "Compares versioned facility geometry and restrictions against vehicle capability."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
      - facility_operator
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - facility_restriction.override
    evaluation_suite: evals/vehicle-facility-compatibility-agent.eval.yaml
  - id: remote-assistance-coordinator
    version: '0.1.0'
    purpose: "Coordinates provider remote-assistance cases. Remote assistance is not remote driving."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
    tenant_scope: isolated
    maximum_autonomy: A3
    allowed_tools: []
    prohibited_actions:
      - remote_drive
      - vehicle.control
    evaluation_suite: evals/remote-assistance-coordinator.eval.yaml
  - id: autonomous-maintenance-coordinator
    version: '0.1.0'
    purpose: "Routes vehicle health summaries into RIGDESK maintenance workflows."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
    tenant_scope: isolated
    maximum_autonomy: A3
    allowed_tools: []
    prohibited_actions:
      - return_to_service.fabricate
      - safety_hold.override
    evaluation_suite: evals/autonomous-maintenance-coordinator.eval.yaml
  - id: autonomous-mission-exception-agent
    version: '0.1.0'
    purpose: "Classifies autonomous mission exceptions and minimal-risk events."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
    tenant_scope: isolated
    maximum_autonomy: A3
    allowed_tools: []
    prohibited_actions:
      - minimal_risk.override
      - vehicle.control
    evaluation_suite: evals/autonomous-mission-exception-agent.eval.yaml
  - id: mission-reconciliation-agent
    version: '0.1.0'
    purpose: "Reconciles completed autonomous missions against commercial and custody records."
    owner: rig-freightos-owner
    module: autonomous_vehicle_gateway
    legal_authority_class: software_only
    operating_contexts:
      - autonomous_mobility
    tenant_scope: isolated
    maximum_autonomy: A2
    allowed_tools: []
    prohibited_actions:
      - billing.change
      - mission_event.fabricate
    evaluation_suite: evals/mission-reconciliation-agent.eval.yaml
