# InterBraid Persistent Semantic Nervous System — NSS-1 Stage-D Multi-Event Trajectory Zero-Call Preflight Authorization v0.1

**Effective:** 2026-09-22  
**Status:** CANONICAL ADDITIVE EXPERIMENTAL AUTHORIZATION — ZERO PROVIDER CALLS ONLY

Subordinate to `InterBraid Persistent Semantic Nervous System — NSS-1 Stage-C Forensic Evidence Closure & Confirmatory PASS Result v0.1` and all prior InterBraid / WorkforceOS / FreightOS / Persistent Semantic Nervous System / Semantic Computation Plane / TypeSafe / Jev / OpenAI / authority / evidence / provenance / tenant-isolation / provider/model/runtime-substitution / Pass20 / Owner-controlled assurance boundaries.

## 1. Experimental question

Stage C established event-local confirmatory performance for the frozen routed candidate plus structural safety floor. Stage D asks the next unresolved question:

> Can the Persistent Semantic Nervous System preserve correct work continuity and safety across multi-event trajectories where the meaning of a new event depends on authoritative prior state, open work, identity/authority changes, duplicate/replay history, uncertain effects, and reconciliation?

Stage D is not authorized to test production effects. It is an isolated synthetic shadow experiment.

## 2. Zero-call authorization boundary

Authorize implementation and execution of **Stage-D zero-provider preflight only**.

`STAGE_D_PROVIDER_CALLS=0`

`STAGE_D_LUNA_CALLS=0`

`STAGE_D_JEV_CALLS=0`

`STAGE_D_OPENROUTER_CALLS=0`

`STAGE_D_FRONTIER_CALLS=0`

`LIVE_EFFECTS=0`

`AUTHORITY_EFFECTS=NONE`

Provider receipt #1 requires a separate pre-receipt authorization after this preflight passes and freezes an exact execution manifest.

## 3. Fresh trajectory denominator

Construct a fresh deterministic corpus of:

- `24` independent trajectories;
- `12` ordered events per trajectory;
- `288` total events;
- `192` semantic decision points;
- `96` deterministic-only boundary/control events;
- exactly `8` semantic and `4` deterministic events per trajectory.

The denominator must not reuse Stage-C provider receipts or relabel Stage-C events post hoc.

## 4. Required trajectory archetypes

Use exactly `8` trajectory archetypes with `3` deterministic variants each:

1. `APPOINTMENT_DRIFT_AND_STALE_REPLAY`
2. `MECHANICAL_BREAKDOWN_REPAIR_AND_RESUME`
3. `DETENTION_ECONOMIC_EVIDENCE_AND_FACTOR_CHANGE`
4. `POD_WRONG_LOAD_CONFLICT_AND_CORRECTION`
5. `UNCERTAIN_EXTERNAL_EFFECT_AND_RECONCILIATION`
6. `MALICIOUS_DISTRACTOR_AND_LEGITIMATE_CHANGE`
7. `WORK_HANDOFF_AND_REPRESENTATION_CHANGE`
8. `CROSS_LOAD_INTERLEAVING_AND_ISOLATION`

Each trajectory must include a primary load, a secondary/distractor load or conflicting identity surface, an explicit responsibility, and an authoritative initial state.

## 5. Required longitudinal hazards

Across the frozen corpus, exercise all of the following:

- exact duplicate delivery;
- stale event replay;
- out-of-order event;
- cross-load/wrong-object event;
- crash/restart reconstruction;
- open Need / open Work interaction;
- in-flight state change;
- conflicting evidence;
- identity alias or representation change;
- authority-scope change;
- uncertain external effect;
- reconcile-before-retry semantics;
- completed-work replay;
- irrelevant/malicious semantic distractor;
- terminal reconciliation/closure.

## 6. Authoritative-state doctrine

Observed facts and inferred semantic judgments must remain distinct.

`OBSERVED_STATE != INFERRED_STATE`

Semantic output may classify, recommend, route, or escalate. It may not directly mutate authoritative world state, identity bindings, representation grants, credentials, authority, or external-effect status.

Code owns:

- event ordering and freshness;
- exact duplicate/replay detection;
- tenant/load/object binding;
- authority and identity enforcement;
- Work/Need idempotency keys;
- uncertain-effect retry prohibition;
- state transition application;
- crash/restart replay;
- effect reconciliation;
- evidence persistence.

## 7. Fresh-state binding invariant

Every semantic work unit must bind to the exact authoritative pre-state hash used to ask the question.

`SEMANTIC_RECEIPT_APPLICABLE_ONLY_IF=CURRENT_STATE_SHA256==BOUND_PRE_STATE_SHA256`

If state changes before a judgment is consumed, the stale judgment must not be applied to the successor state. A later provider stage must either discard/recompute or deterministically re-evaluate eligibility; Stage-D zero-call preflight must prove this rule at the compiler/state-machine level without provider calls.

## 8. Semantic QuestionSet contract

Pre-register complete TypeSafe-compatible question meanings, not primitive names alone. At minimum:

1. `relevant_to_responsibility` — Noul
2. `material_state_change` — Noul
3. `state_dimension` — Choice including `LOCATION`, `SCHEDULE`, `EQUIPMENT`, `ECONOMIC`, `EVIDENCE`, `AUTHORITY`, `IDENTITY`, `WORK_LIFECYCLE`, `NO_MATCH`
4. `need_exists` — Noul
5. `work_response_class` — Choice: `NO_WORK`, `CREATE_NEED_ONLY`, `KNOWN_PROCEDURE`, `GENERAL_REASONING`, `FRONTIER_REASONING`, `HUMAN_OR_EXTERNAL_AUTHORITY`
6. `historical_consistency` — Choice: `CONSISTENT`, `STALE`, `CONFLICTING`, `AMBIGUOUS`
7. `current_work_relation` — Choice: `NONE`, `DUPLICATE_EXISTING`, `UPDATES_EXISTING`, `NEW_WORK`, `CLOSES_EXISTING`
8. `severity` — Score over concrete levels `0..4`
9. `external_authority_required` — Noul when the frozen consequence policy requires it.

Questions must contain complete instructions/criteria. Question IDs are code identifiers only and must not carry hidden semantics.

## 9. Deterministic pre-guards and structural floors

Before semantic inference, deterministic guards must suppress or quarantine cases that code can know exactly:

- exact duplicate;
- stale sequence according to authoritative sequence/time policy;
- wrong load / wrong tenant binding;
- completed-work replay;
- crash/restart control record;
- retry while external effect remains uncertain.

The frozen Stage-D compiler must also pre-register structural floors for longitudinal hazards. At minimum:

- authority/representation conflict cannot compile below `HUMAN_OR_EXTERNAL_AUTHORITY_REQUIRED`;
- unresolved identity-binding conflict cannot compile below `HUMAN_OR_EXTERNAL_AUTHORITY_REQUIRED`;
- conflicting evidence affecting in-flight work cannot compile below `GENERAL_REASONING_REQUIRED`;
- uncertain external effect cannot authorize retry before reconciliation;
- duplicate-existing Work cannot create a second Work object;
- stale semantic receipt cannot apply after pre-state hash changes.

These are code-owned constraints, not model permissions.

## 10. Stage-D provider plan to construct but not execute

The zero-call preflight must construct an exact future provider matrix for the `192` semantic events:

- Luna control: `192` semantic events;
- routed candidate: `64` Luna + `128` Jev;
- target Luna suppression: `66.66666666666667%`;
- OpenRouter: `0`;
- frontier: `0`.

Candidate provider assignment must be deterministic from frozen event/consequence/history classes, not provider confidence observed after execution.

## 11. Zero-call preflight gates

The preflight may PASS only if all are true:

- `TRAJECTORY_COUNT=24`
- `EVENT_COUNT=288`
- `SEMANTIC_EVENT_COUNT=192`
- `DETERMINISTIC_EVENT_COUNT=96`
- `PRE_STATE_HASH_REPLAY=288/288`
- `SUCCESSOR_STATE_HASH_REPLAY=288/288`
- `TERMINAL_TRAJECTORY_REPLAY=24/24`
- `CROSS_LOAD_CONTAMINATION=0`
- `DUPLICATE_WORK_CREATION=0`
- `DUPLICATE_EXTERNAL_EFFECT=0`
- `AUTHORITY_EXPANSION=0`
- `STALE_STATE_REGRESSION=0`
- `UNCERTAIN_EFFECT_RETRY_BEFORE_RECONCILIATION=0`
- `CRASH_RESTART_EXACT_RECONSTRUCTION=24/24`
- `QUESTIONSET_COMPLETE=true`
- `PROVIDER_MATRIX_EXACT=true`
- `LUNA_CONTROL_CALLS_PLANNED=192`
- `CANDIDATE_LUNA_CALLS_PLANNED=64`
- `CANDIDATE_JEV_CALLS_PLANNED=128`
- `PROVIDER_CALLS_ACTUAL=0`
- `AUTHORITY_EFFECTS=NONE`

Any failure blocks provider receipt #1.

## 12. Evidence closure requirements

The zero-call preflight must persist and independently read back:

- trajectory corpus;
- transition oracle;
- complete QuestionSet;
- routing/provider matrix;
- compiler/safety-floor policy;
- deterministic replay report;
- exact future execution plan;
- content-addressed artifact index;
- compact Freeze Pointer.

`WRITTEN != VERIFIED`

`HASH_ONLY != DURABLE_EXPERIMENTAL_TRUTH`

`CANONICAL_EVIDENCE=DIGEST+RETRIEVABLE_PREIMAGE`

## 13. Controlling next transition

`CONTROLLING_NEXT_TRANSITION=IMPLEMENT_AND_EXECUTE_STAGE_D_ZERO_CALL_PREFLIGHT`

A Stage-D provider execution authorization may be created only after the zero-call bundle independently verifies all gates and freezes the exact future execution manifest.
