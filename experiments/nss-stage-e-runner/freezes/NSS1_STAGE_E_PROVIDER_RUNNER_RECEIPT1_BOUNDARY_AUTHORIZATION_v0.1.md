# InterBraid Persistent Semantic Nervous System — NSS-1 Stage-E Provider Runner Receipt-1 Boundary Authorization v0.1

Effective: 2026-09-22

Status: CANONICAL ADDITIVE PRE-RECEIPT EXECUTION AUTHORIZATION

Subordinate to `InterBraid Persistent Semantic Nervous System — NSS-1 Stage-E Persistent Concurrent Event-Stream Zero-Call Result v0.1`, `InterBraid Persistent Semantic Nervous System — NSS-1 Stage-E Pre-Receipt Provider Shadow Execution Authorization v0.1`, all prior NSS / Semantic Computation Plane / TypeSafe-Jev / WorkforceOS / InterBraid authority / evidence / provenance / tenant-isolation / provider-model-runtime substitution / Pass20 and Owner-controlled assurance state, and separately preserving the Stage-C interruption/evidence-closure state without reinterpretation.

## Runner preflight result

- `NSS1_STAGE_E_PROVIDER_RUNNER_PREFLIGHT=PASS`
- `PROVIDER_RECEIPTS=0`
- `ISSUES=[]`
- `AUTHORITY_EFFECTS=NONE`
- `LIVE_EFFECTS=0`
- `OPENROUTER_CALLS=0`
- `FRONTIER_CALLS=0`
- Runner deployment: `aa1cc516-ef87-4439-94d9-bb5d62639fe3`
- Runner source commit: `f5bcd7dc721e7f4088c9053e2c5b445e563c3f06`

## Exact frozen identities

- Stage-E provider runner execution manifest SHA-256: `9b477dbb495d6aa3c890a4177efcbe35b18aa0e87691b1801aac135bebdea38b`
- Zero-call execution manifest SHA-256: `517f2fada5fe54dc22f21fff3b26d55a48e874bf5b08b46225287261e533062a`
- Zero-call Freeze Pointer SHA-256: `c7c7863c7d858a85ac7871b0f367ec7d3d2a0c665d84dcf7dc5119113b375d56`
- Provider Matrix SHA-256: `76eb87303b6307c5fd7d67c3719b78989d05ee34dbe5bbf7356071f1f1a20b28`
- Question Set SHA-256: `c007ae5b13c152a93432adb6491fd59d156bfc54a2c14bb537ff77af13edabb2`

## Frozen denominator and provider schedule

- total events: `384`
- semantic events in corpus: `288`
- provider-eligible semantic attempts: `240`
- control Luna calls: `240`
- candidate-shared Luna receipts: `80`
- candidate Jev calls: `160`
- maximum distinct provider attempts: `400`
- candidate Luna suppression: `66.6666666667%`

## Frozen model identities

- Luna requested model: `gpt-5.6-luna`
- Luna required effective model: `gpt-5.6-luna`
- Jev requested model: `jev-latest`
- Jev required effective model: `jev-1.13.0`

No post-receipt widening of accepted provider identity is permitted.

## TypeSafe / System-One execution doctrine

The TypeSafe working contract remains controlling: code owns workflow, deterministic facts, state transitions, permissions, and execution; Jev supplies typed semantic judgments only. Choice, Noul, and Score retain their distinct semantics. Independent questions over the same state may be batched, but model output is not ground truth, confidence is not authority, and freshness must be checked before any result is consumed against changed state.

Stage-E therefore treats all provider output as `NON_AUTHORITATIVE_SHADOW_JUDGMENT`.

## Receipt-1 authorization boundary

The exact runner manifest above is now authorized for one isolated Stage-E shadow provider execution candidate. Before any network call, the execute-capable runner must re-derive and re-emit the same runner execution manifest SHA-256 and must observe zero pre-existing provider receipts in `/data/nss1-stage-e-provider-v0.1`.

Receipt #1 may be crossed only if those checks pass exactly.

After receipt #1:

- corpus mutation is prohibited;
- semantic-attempt mutation is prohibited;
- QuestionSet mutation is prohibited;
- provider-matrix mutation is prohibited;
- model-identity mutation is prohibited;
- state-binding mutation is prohibited;
- retry-policy mutation is prohibited;
- metrics/gate mutation is prohibited;
- source/redeployment is prohibited during active execution except for a separately frozen fail-closed recovery transition.

## Evidence and uncertain-effect rules

Every provider call must have a durable attempt artifact written before dispatch. Raw provider response bytes must be persisted before normalized terminal receipt closure. Terminal receipts must bind request identity, provider/model identity, event identity, bound state hash, QuestionSet, raw-response hash, usage, latency/cost when available, normalized typed judgments, and terminal status.

A provider attempt observed as `STARTED` without an attributable terminal receipt is `ORCHESTRATION_UNCERTAIN`; automatic retry is prohibited. Post-hoc provider replay is prohibited as a substitute for missing original evidence.

## Fresh-state rule

`MODEL_COMPLETION != CURRENT_TRUTH`

A semantic receipt may be classified as fresh only when its frozen `bound_state_sha256` equals the authoritative state hash at the deterministic completion point defined by the frozen Stage-E schedule. Otherwise it is a stale historical judgment and must be rejected from current-state application. Because Stage-E is shadow-only, no provider judgment is permitted to mutate authoritative state in either case.

## Prohibited

No production activation, live freight action, customer action, dispatch/booking, live payment, custody/transmission, financing, production signing, external counterparty mutation, OpenRouter/frontier escalation, or authority expansion.

## Controlling next transition

`CONTROLLING_NEXT_TRANSITION=IMPLEMENT_EXECUTE_CAPABLE_RUNNER_WITH_EXACT_MANIFEST_PRESERVATION_AND_REPRECHECK_BEFORE_RECEIPT1`

This freeze authorizes implementation and one isolated shadow execution only within the exact boundary above. It does not itself claim provider semantic PASS.