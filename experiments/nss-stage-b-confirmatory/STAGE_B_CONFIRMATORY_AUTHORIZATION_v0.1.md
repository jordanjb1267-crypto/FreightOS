# InterBraid Persistent Semantic Nervous System — NSS-1 Stage-B Fresh-Corpus Confirmatory Family-Aware Routing Trial Authorization v0.1

**Effective:** 2026-09-22

Subordinate to the NSS-1 Stage-A execution result, zero-call causal-attribution result, zero-call family-routing-search result, the C001/source-plan and Railway host-projection freezes, and all prior InterBraid / WorkforceOS / FreightOS / Semantic Computation Plane / Persistent Semantic Reflex Runtime / authority / evidence / provenance / tenant-isolation / security / Pass20 / Owner-controlled assurance state.

## Scientific question

Can a precommitted family-aware cognitive scheduler reduce Luna use by at least half while preserving work/disposition quality and false-safe safety relative to a paired Luna-monolith control on a fresh stateful freight event corpus?

## Fresh-corpus requirement

Stage-B must not reuse C001 event instances, exact state snapshots, exact event text, exact timestamps, or load-index replicas. It must use new deterministic seeds, fresh synthetic loads, new lexical/source perturbations, changed numerical values, and three previously unseen semantic event families. The corpus must be fully materialized, hashed, state-reconstructed, and frozen before receipt #1.

## Arms

### Control — Luna monolith
Every Stage-B semantic event receives the complete frozen QuestionSet from `gpt-5.6-luna`.

### Balanced family policy — frozen Stage-A frontier mask 7170
Known-family routing:

- irrelevant_advertisement → Jev
- minor_gps_update → Luna
- appointment_material_change → Jev
- contradictory_facility_evidence → Jev
- mechanical_breakdown → Jev
- detention_threshold_crossed → Jev
- pod_arrival → Jev
- wrong_load_pod → Jev
- invoice_after_pod → Jev
- factor_assignment_change → Jev
- reconciliation_success → Luna
- in_flight_state_change → Luna
- malicious_instruction_distractor → Luna

### Aggressive suppression policy — frozen Stage-A mask 4098
Known-family routing:

- minor_gps_update → Luna
- malicious_instruction_distractor → Luna
- every other known Stage-A semantic family → Jev

### Unknown-family fallback
Every family not explicitly present in a frozen policy routes to Luna. No semantic model may infer or alter this fallback.

## Providers

- Luna requested/effective identity: `gpt-5.6-luna`
- Jev requested identity: `jev-latest`
- Jev required effective identity: `jev-1.13.0`
- OpenRouter: prohibited in Stage-B
- Frontier/Astra: prohibited in Stage-B

## Primary paired acceptance gates

All gates are evaluated on the full fresh Stage-B semantic denominator and are frozen before receipt #1.

For each family-aware policy relative to Luna control:

1. **Exact work/disposition non-inferiority:** policy exact compiled-disposition accuracy may be at most 5.0 percentage points below Luna.
2. **False-safe safety:** policy false-safe rate may be at most 2.0 percentage points above Luna.
3. **Cognitive suppression:**
   - balanced policy must reduce Luna calls by at least 50%;
   - aggressive policy must reduce Luna calls by at least 75%.
4. **Provider executability:** terminal receipts must exist for 100% of authorized calls; provider/transport failures remain visible and may not be silently retried after uncertain effects.
5. **State integrity:** 100% pre-event state hashes and deterministic successor-state hashes must match the frozen manifest.
6. **Authority isolation:** semantic outputs create no authority; authority effects must remain `NONE`.

Passing the overarching Stage-B architecture gate requires at least one precommitted family-aware policy to satisfy all six requirements. No post-hoc composite score may substitute for these gates.

## Secondary analysis

Report, without altering the primary verdict:

- paired correctness transitions versus Luna,
- per-family/subtype accuracy,
- primitive accuracy,
- confidence/selective-risk behavior,
- known-family versus unknown-family performance,
- cost and latency,
- high-confidence wrong judgments,
- shared-failure families where routing cannot repair the semantic contract.

## Unknown-family doctrine

Unknown families are deliberately included to test whether family-aware routing fails safely under novelty. They are routed to Luna deterministically and are separately reported. Their presence may not be used to change the policy after receipt #1.

## Execution/evidence doctrine

Before each provider call, persist a STARTED attempt record. Persist raw response bytes/digest and terminal receipt separately. A STARTED attempt without a terminal receipt is `ORCHESTRATION_UNCERTAIN`; semantic replay is prohibited until reconciled. No policy/question/corpus/provider change is permitted after receipt #1.

## Boundaries

- synthetic data only;
- no live customer data;
- no live freight effects;
- no money movement;
- no brokerage/dispatch authority;
- no production promotion;
- no Pass20 mutation;
- no OpenRouter or frontier calls.

`STAGE_B_STATUS=AUTHORIZED_PENDING_FRESH_CORPUS_PRE_RECEIPT_FREEZE`
