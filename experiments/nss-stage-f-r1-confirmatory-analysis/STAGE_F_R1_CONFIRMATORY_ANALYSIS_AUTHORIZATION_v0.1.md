# InterBraid Persistent Semantic Nervous System — NSS-1 Stage-F R1 Gold-Bearing Confirmatory Quality / Safety Analysis Authorization v0.1

Effective: 2026-09-23

Status: CANONICAL ADDITIVE ZERO-PROVIDER ANALYSIS AUTHORIZATION

Subordinate to the immutable Stage-F gold-bearing confirmatory authorization and zero-call evidence root, the Stage-F predecessor 400-receipt ledger, the Jev 422 forensic diagnosis, the R1 Jev contract-remediation authorization/execution, `NSS1-STAGE-F-R1-PHASE-V-v0.3`, and all prior NSS / Semantic Computation Plane / WorkforceOS / InterBraid authority, evidence, provenance, tenant-isolation, provider-substitution, security and Pass20 assurance state.

## 1. Purpose

This analysis answers the confirmatory question Stage F was designed to answer after the Jev request-contract defect was remediated:

> Does the frozen family/subtype-aware candidate preserve semantic and work-outcome quality and false-safe safety while suppressing Luna usage, when evaluated against the pre-receipt Stage-F gold?

No provider execution is authorized by this analysis.

## 2. Frozen evidence inputs

The analyzer MUST independently bind to:

- verified Stage-F evidence archive SHA-256 `b43f7bb6f2a158300c63ff0df9519de21a18e1005860490f4f97ac907ba52af8`;
- Stage-F predecessor receipt index SHA-256 `2e50185fa5d4ee5c3f137ccfbb4283d5a8964e6e3d53288a29043ee8f9d207e7`;
- R1 Jev receipt index SHA-256 `b47252de25cc86dc6bf94f4c52ac7f44f21faabb1db13a41801400b35944b900`;
- R1 runner manifest SHA-256 `a25b395fb70a571bea8711d1354a6032772a553468da8d005100aed4f3b386b8`;
- R1 Phase-V verified result SHA-256 `7901d874dae0b803b0d6a341d296fb7aadf26616aa8f629f0098726440a1eb54`.

Frozen Stage-F inner roots MUST match their canonical hashes, including QuestionSet, provider matrix, semantic gold, Need gold, work-disposition gold, consequence gold/weights, known/novel classification, freshness gold, authority expectations, metric specification and acceptance gates.

## 3. Control and candidate reconstruction

Control policy:

`CONTROL = original Stage-F Luna receipt for all 240 semantic events`.

Candidate policy:

- 160 `KNOWN` events: verified R1 Jev receipt for the exact predecessor Jev event ID;
- 80 `NOVEL` events: the original canonical Luna receipt is shared with control.

No second Luna sample is created.

The candidate therefore contains exactly 240 semantic observations while using 80 Luna observations and 160 Jev observations.

Realized Luna suppression remains `160 / 240 = 66.6666666667%`.

## 4. No replay / immutability

The analyzer MUST make:

- Luna calls: 0;
- Jev calls: 0;
- OpenRouter calls: 0;
- frontier calls: 0.

It MUST read no provider credential and MUST not mutate either source ledger.

Source-ledger fingerprints MUST be computed before and after analysis and must remain identical.

## 5. Confirmatory metrics

Where mathematically determined by the frozen artifacts, independently compute for control and candidate:

- Noul Brier score and log loss against frozen binary semantic gold;
- Choice exact accuracy against frozen Choice gold;
- Score MAE against frozen severity gold;
- work-disposition exact accuracy;
- false-safe rate using the frozen work-response ordering, where predicted ordering below required ordering is false-safe;
- false-escalation rate where predicted ordering exceeds required ordering;
- C4 false-safe count;
- provider executability;
- state-binding integrity;
- stale-current-state applications;
- Luna suppression;
- latency distributions;
- usage/cost evidence when actually persisted;
- known/novel and family/consequence-class breakdowns.

## 6. Need precision / recall decision-rule integrity

Stage-F froze binary `NEED_GOLD` and a Noul probability for `need_exists`, but the frozen `metric_spec.json` and `acceptance_gates.json` do not themselves state a probability-to-binary decision threshold.

The analyzer MUST NOT choose a threshold after observing provider results.

Unless a controlling pre-receipt artifact explicitly supplies the decision rule, `NEED_PRECISION`, `NEED_RECALL`, and the acceptance gate `NEED_RECALL_DELTA_VS_CONTROL>=-5pp` MUST be reported as `NOT_EVALUABLE_MISSING_PRE_REGISTERED_DECISION_RULE`.

This condition prevents a full Stage-F architecture PASS. It is an experiment-identifiability limitation, not evidence of candidate semantic failure.

For descriptive analysis only, continuous `need_exists` Brier/log-loss remains permitted because it requires no classification threshold.

## 7. Gate semantics

Evaluate each frozen acceptance gate as `PASS`, `BLOCK`, or `INCONCLUSIVE`.

A full architecture `PASS` requires every applicable pre-registered gate to be evaluable and satisfied.

If all evaluable gates pass but the Need-recall gate is non-identifiable, the controlling architecture verdict is:

`INCONCLUSIVE_MISSING_PRE_REGISTERED_NEED_DECISION_RULE`.

If any evaluable gate fails, the verdict is `BLOCK` regardless of the missing Need threshold.

No post-receipt threshold selection, gate relaxation, denominator change, gold mutation, receipt deletion, or model substitution is permitted.

## 8. Authority boundary

`AUTHORITY_EFFECTS=NONE`

`LIVE_FREIGHT_EFFECTS=0`

`LIVE_MONEY_MOVEMENT=0`

`PRODUCTION_PROMOTION=false`

All model outputs remain historical shadow evidence only.

## 9. Controlling next transition

`CONTROLLING_NEXT_TRANSITION=STAGE_F_R1_ZERO_PROVIDER_GOLD_BEARING_CONFIRMATORY_ANALYSIS_PHASE_A`

Phase A may write only derived analysis evidence. After a clean write-complete state, a separately executed independent zero-call Phase V must reread the same source ledgers and frozen gold and recompute the result without importing the Phase-A analyzer.
